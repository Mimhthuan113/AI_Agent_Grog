# Smart AI Home Hub — Backend Dependencies
# ==========================================
# Install: pip install -r requirements.txt

# ── Web Framework ────────────────────────────
fastapi==0.115.8
uvicorn==0.34.0
python-multipart==0.0.26

# ── Validation & Config ─────────────────────
pydantic==2.12.5
pydantic-settings==2.14.0
python-dotenv==1.0.1

# ── Authentication ───────────────────────────
PyJWT==2.12.1
bcrypt==5.0.0
passlib==1.7.4
cryptography==46.0.5
google-auth==2.38.0
requests==2.32.3

# ── HTTP Client (Groq API) ──────────────────
httpx==0.28.1

# ── Database ─────────────────────────────────
aiosqlite==0.22.1
redis==7.4.0

# ── UI Automation (Generic UI Agent) ─────────
# Cho phép Aisha tự động hóa BẤT KỲ app nào trên Windows.
# Dùng Vision LLM (Groq) để nhìn màn hình → click, type, paste.
pyautogui>=0.9.54        # Điều khiển chuột, bàn phím, chụp màn hình
pyperclip>=1.9.0         # Clipboard — paste tiếng Việt an toàn
Pillow>=10.0.0           # Resize ảnh trước khi gửi Vision LLM
pygetwindow>=0.0.9       # Tìm và focus cửa sổ app (Windows)

# ── Voice / TTS ──────────────────────────────
edge-tts>=7.0.0          # Giọng Microsoft vi-VN-HoaiMyNeural
